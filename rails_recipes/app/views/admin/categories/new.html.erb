<h1>new category</h1>

<%= form_for @category, :url => admin_categories_path do |f| %>
  <%= f.label :name, "name" %>
  <%= f.text_field :name %>

  <%= f.submit "create" %>
<% end %>

<p><%= link_to "cancel", admin_categories_path, :class => "btn btn-primary" %></p>